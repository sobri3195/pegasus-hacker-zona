import {ChevronUp} from 'lucide-react';import {useState} from 'react';import {useWorkspaceStore} from '../../../stores/workspaceStore';
export function HistoryTray(){const [open,setOpen]=useState(true);const history=useWorkspaceStore(s=>s.history);return <section className={`${open?'h-36':'h-10'} shrink-0 border-t border-white/[.06] bg-[#07101a] transition-[height]`}><button className="flex h-10 w-full items-center px-5 text-[9px] font-semibold tracking-[.2em] text-slate-500" onClick={()=>setOpen(v=>!v)}>COMMAND EXECUTION HISTORY <span className="ml-2 rounded bg-white/[.04] px-1.5 py-0.5 text-cyan-400">{history.length}</span><ChevronUp size={13} className={`ml-auto transition ${open?'':'rotate-180'}`}/></button>{open&&<div className="grid grid-cols-[72px_1fr_90px_90px_70px] gap-3 overflow-y-auto px-5 pb-3 text-[10px]">{history.slice(0,4).map(item=><div className="contents" key={item.id}><span className="font-mono text-slate-600">{new Date(item.timestamp).toLocaleTimeString('en-GB',{hour12:false})}</span><span className="truncate font-mono text-slate-300">{item.raw}</span><span className={item.status==='SUCCESS'?'text-emerald-400':item.status==='BLOCKED'?'text-amber-300':'text-red-400'}>{item.status}</span><span className="truncate text-slate-600">{item.provider}</span><span className="text-right text-slate-600">{item.duration} ms</span></div>)}</div>}</section>}

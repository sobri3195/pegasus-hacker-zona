import {Command} from 'lucide-react'; import {useEffect,useState} from 'react'; import {commandRegistry} from '../commandRegistry'; import {useWorkspaceStore} from '../../../stores/workspaceStore';
export function CommandPalette({onSelect}:{onSelect:(value:string)=>void}){const {paletteOpen,setPaletteOpen}=useWorkspaceStore();const [query,setQuery]=useState('');useEffect(()=>{const handler=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==='k'){event.preventDefault();setPaletteOpen(true)}if(event.key==='Escape')setPaletteOpen(false)};window.addEventListener('keydown',handler);return()=>window.removeEventListener('keydown',handler)},[setPaletteOpen]);if(!paletteOpen)return null;const filtered=commandRegistry.filter(c=>`${c.name} ${c.description}`.toLowerCase().includes(query.toLowerCase())).slice(0,8);return <div className="fixed inset-0 z-50 flex justify-center bg-black/70 px-4 pt-[14vh] backdrop-blur-sm" onMouseDown={()=>setPaletteOpen(false)}><div role="dialog" aria-modal="true" aria-label="Command palette" onMouseDown={e=>e.stopPropagation()} className="h-fit w-full max-w-xl overflow-hidden rounded-xl border border-cyan-300/20 bg-[#0a1520] shadow-2xl shadow-cyan-950"><div className="flex items-center gap-3 border-b border-white/[.07] p-4"><Command size={18} className="text-cyan-300"/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search authorized commands..." className="flex-1 bg-transparent text-sm text-white outline-none placeholder:text-slate-600"/><kbd className="rounded border border-white/10 px-2 py-1 text-[9px] text-slate-500">ESC</kbd></div><div className="p-2">{filtered.map(command=><button key={command.name} onClick={()=>{onSelect(command.name+' ');setPaletteOpen(false)}} className="flex w-full items-center gap-4 rounded-lg px-3 py-3 text-left hover:bg-cyan-300/[.06]"><span className="w-20 font-mono text-xs text-cyan-300">{command.name}</span><span className="flex-1 text-xs text-slate-400">{command.description}</span><span className="text-[9px] tracking-wider text-slate-600">{command.category}</span></button>)}</div></div></div>}

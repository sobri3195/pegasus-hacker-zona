import {describe,expect,it} from 'vitest';import {createDocumentIntelligence,supportedDocumentExtensions} from '../documentService';
describe('document intelligence',()=>{it('supports every requested format',()=>expect(supportedDocumentExtensions).toEqual(['pdf','docx','xlsx','pptx','txt','html']));it('preserves evidence and separates derivative analysis',()=>{const result=createDocumentIntelligence('brief.pdf','analyze');expect(result.metadata.sha256).toMatch(/^[a-f0-9]{64}$/);expect(result.evidence).toMatchObject({originalHashed:true,sourceModified:false});expect(result.evidence.derivativeId).not.toBe(result.evidence.id)});it('labels anomalies without claiming tampering',()=>{const result=createDocumentIntelligence('brief.docx','metadata');expect(result.anomalies.every(a=>a.label==='ANOMALY'&&a.review==='REQUIRES REVIEW')).toBe(true);expect(result.disclaimer).toContain('do not establish tampering')});it('rejects unsupported files',()=>expect(()=>createDocumentIntelligence('payload.exe','analyze')).toThrow('Unsupported document type'))});
